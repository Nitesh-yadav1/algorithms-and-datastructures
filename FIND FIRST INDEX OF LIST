def firstindexbetter(a,x,si): #recomended
    l=len(a)
    if si==l:
        return -1
    if a[si]==x:
        return si
    output=firstindexbetter(a,x,si+1)
    return output
a=[1,2,4,5,6,7,8,9,7]
print(firstindexbetter(a,7,0))
        
